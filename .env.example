# Extracta — server fino. El documento se procesa 100% en el navegador y NUNCA llega acá.
# Copiá este archivo a .env y completá los valores. El .env NO va al repo.

PORT=3000

# --- Login ---
# AUTH_ENABLED=false → la app se sirve sin clave (uso local / red privada).
AUTH_ENABLED=true
AUTH_USER=diego

# Hash bcrypt de tu contraseña (NO la contraseña en texto plano). Generalo con:
#   node server.js --hash 'tu-contraseña'
AUTH_PASSWORD=

# Secreto para firmar la cookie de sesión. Generalo con:
#   openssl rand -hex 32
SESSION_SECRET=

# Duración de la sesión, en horas.
SESSION_TTL_HOURS=12

# Cookie Secure: true detrás de HTTPS (EasyPanel). Poné false solo para probar local por http://.
COOKIE_SECURE=true
